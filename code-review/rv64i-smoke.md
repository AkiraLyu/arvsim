# 平台集成测试精简

集成层保留实际汇编程序执行和整机复位两个用例。删除重复的单条 `addi`、UART 寄存器、Virtio 队列大小和总线边界检查；这些行为仍由对应 CPU、设备和总线模块测试覆盖。

整机复位测试保留，因为它验证设备和 RAM 在同一生命周期中的协作。程序测试通过正式装载器运行工具链生成的镜像，预期是客体签名与架构异常，而非内部函数返回值。详见 [测试说明](../docs/repository-status/rv64i-smoke.md)。
